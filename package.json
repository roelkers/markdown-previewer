{
  "name": "markdown-previewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-markdown": "^2.5.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "homepage" : "https://roelkers.github.io/markdown-previewer",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "consumedServices": {
  "autosave": {
    "versions": {
      "0.24.0": "consumeAutosave"
    }
  }
}
}
